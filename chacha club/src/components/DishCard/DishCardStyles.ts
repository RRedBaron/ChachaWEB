import { Typography } from "@mui/material";
import { styled } from "@mui/system";

export const StyledDishCardTitle = styled(Typography)({
  fontSize: "1.5rem",
  textAlign: "center",
  marginBottom: "10px",
});

export const StyledDishCardPrice = styled(Typography)({
  fontSize: "1.5rem",
  textAlign: "center",
  fontWeight: "bold",
});

export const StyledDishCardDetails = styled(Typography)({
  fontSize: "1.25rem",
  textAlign: "center",
});
